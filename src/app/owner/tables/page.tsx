import { Placeholder } from "@/ui/placeholder";
export const metadata = { title: "Tables" };
export default function Page() {
  return (
    <Placeholder
      owner
      title="Tables"
      description="Manage tables and their ordering codes."
    />
  );
}
